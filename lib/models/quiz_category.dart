class QuizCategory {
  String? name;
  int? id;
  String? difficulity;

  QuizCategory(this.name, this.id, this.difficulity);
}

class News {
  String? title;
  String? image;

  News(this.title, this.image);
}
